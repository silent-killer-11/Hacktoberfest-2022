#include <bits/stdc++.h>
using namespace std;

int main(){
    string name, surname;

    cin>>name;
    cin>>surname;

    string fullname = name + " " + surname;
    cout<<"Fullname is: "<<fullname;
    return 0;
}